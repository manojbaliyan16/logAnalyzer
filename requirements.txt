# LogAnalyser Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies (required)
jira>=3.0.0
tabulate>=0.8.9
selenium>=4.0.0
pandas>=1.3.0  # For data processing
openpyxl>=3.0.0  # For Excel file handling (.xlsx)
xlrd>=1.2.0  # For reading old .xls Excel files
xlsxwriter>=1.3.0  # For writing Excel files
requests>=2.25.0
datefinder>=0.7.0  # For date extraction from text

# Optional dependencies for new log types
python-dlt>=0.1.0  # For DLT file processing (optional, can use dlt-convert CLI instead)

# Development dependencies (optional)
# pytest>=7.0.0  # For running tests
# pytest-cov>=4.0.0  # For coverage reports
